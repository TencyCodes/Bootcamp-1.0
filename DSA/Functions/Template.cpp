#include <bits/stdc++.h>
using namespace std;

// Fast IO
#define fastio()                 \
    ios::sync_with_stdio(false); \
    cin.tie(NULL);               \
    cout.tie(NULL)

// Solve Function
void solve()
{
}

int main()
{
    fastio();

    int t = 1;
    cin >> t; // comment this line if single test case
    while (t--)
    {
        solve();
    }

    return 0;
}

for (int i = 0; i < n;i++){
    if(arr[i]==)
}